@using System.Globalization

<MudSelect T="CultureInfo" Label="@Localizer["Culture"]" Variant="Variant.Text" Dense="true" @bind-Value="Culture">
    @foreach (var culture in cultures)
    {
        <MudSelectItem T="CultureInfo" Value="@culture.Key">@culture.Value</MudSelectItem>
    }
</MudSelect>